"use client";

import { ActivityLogBell } from "./ActivityLogBell";
import { UserCircle } from "lucide-react";

export function Header() {
  return null; // Empty - we'll integrate directly into layout
}
